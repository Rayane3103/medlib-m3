# Dependencies
node_modules

# Build output
dist
dist-ssr
*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Rust / Tauri
src-tauri/target
src-tauri/gen/schemas

# Built phone-scanner bundle (generated by `npm run build:mobile`)
src-tauri/mobile-scanner

# Local database files (never commit real store data)
*.db
*.db-shm
*.db-wal

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Updater signing key - lives in ~/.tauri/ and in GitHub secrets, never here
*.key
*.key.pub
*.key.password
.env

# Claude Code session files
.claude/scheduled_tasks.lock

# Per-machine cargo settings (e.g. target-dir on another drive)
src-tauri/.cargo/config.toml
